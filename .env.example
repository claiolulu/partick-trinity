# 复制成 .env 再填。.env 已经在 .gitignore 里，不会被提交。
#
# 注意：spike/ 是纯静态页面，没有构建步骤，所以 .env 不会被自动注入。
# spike/mapbox.html 在浏览器里拿 token，两条路：
#   1. 打开 spike/mapbox.html?token=pk.xxx —— 读到之后立刻从地址栏抹掉并存进 localStorage
#   2. 直接在页面的输入框里填
# 这么做是因为这一页要部署到 GitHub Pages：任何写进仓库的 token 都等于公开。
#
# 建议用一个 scope 只有 styles:read / fonts:read 的公开 token，
# 并且在 Mapbox 后台给它配上 URL 白名单。
MAPBOX_TOKEN=pk.your_token_here
